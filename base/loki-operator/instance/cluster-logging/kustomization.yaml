---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

#generatorOptions:
#  disableNameSuffixHash: true
#
#configMapGenerator:
#  - name: user-ca-bundle
#    files:
#      - service-ca.crt=bundle.pem
#      - ca-bundle.crt=bundle.pem

resources:
  - namespace.yaml
  - sealed-secret.yaml
  - cluster-role-log-viewers.yaml
  - loki-stack.yaml
